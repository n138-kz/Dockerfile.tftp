name: Docker Setup Compose
on:
  workflow_dispatch:
  push:
jobs:
  build:
    runs-on:
      - ubuntu-latest
    steps:
      - run: pwd;tree
      - name: "compose config"
        run: docker compose config

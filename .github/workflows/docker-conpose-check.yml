name: Docker Setup Compose
on:
  workflow_dispatch:
  push:
jobs:
  build:
    runs-on:
      - ubuntu-latest
    steps:
      - run: git pull
      - name: "compose config"
        run: docker compose config

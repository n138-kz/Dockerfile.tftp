name: Docker Setup Compose
on:
  workflow_dispatch:
  push:
jobs:
  build:
    runs-on:
      - ubuntu-latest
    steps:
      - run: tree
      - name: "compose config"
        run: docker compose config
